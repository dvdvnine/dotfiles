#!/usr/bin/env bash
#
# Launch a TUI app through `xdg-terminal-exec`.

set -euo pipefail

(( $# )) || {
  echo "Usage: ${0##*/} <command> [args...]" >&2
  exit 2
}

app="$1"
shift

exec xdg-terminal-exec "--app-id=local.${app}" -- "${app}" "$@"
